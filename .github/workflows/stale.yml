name: Close stale issues

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

permissions:
  issues: write

jobs:
  stale:
    uses: ljmerza/misc-actions/.github/workflows/stale-issues.yml@main
